#This file is manually set to JDK 17 for CI compatibility.
# Java 17 (LTS) is pre-installed on CI runners. Foojay download URLs are not needed.
# Raise to 21+ once CI infrastructure supports a newer JDK or the project is migrated.
toolchainVersion=17
